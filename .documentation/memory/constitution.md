# DataSpark Constitution

> Generated by `/speckit.discover-constitution` on 2026-03-30 from analysis of 119 source files and 126 test methods.

## Core Principles

### I. Clean Architecture — Core-First (MANDATORY)

All business logic, database access, and data transformation MUST reside in `DataSpark.Core`.
The `DataSpark.Web` and `DataSpark.Console` projects are presentation layers only — they wire up DI,
handle HTTP/CLI concerns, and delegate all work to Core services.

- (MUST) No database queries in Web controllers or Console commands
- (MUST) No business logic in ViewModels, Views, or Presentation classes
- (MUST) `DataSpark.Core` has zero dependencies on `DataSpark.Web` or `DataSpark.Console`
- (MUST) New features start as Core service interfaces before any presentation wiring

**Evidence**: `DataSpark.Core/Services/` contains all 18 service implementations; controllers
contain only thin delegation to Core services.

### II. Testing Standards (MANDATORY)

All production code in `DataSpark.Core` MUST have corresponding tests in `DataSpark.Tests`.
The CI pipeline MUST enforce an 80% minimum coverage floor.

- (MUST) Test framework: MSTest with `[TestMethod]`, `[TestClass]`, `[TestInitialize]`, `[TestCleanup]`
- (MUST) Assertion library: FluentAssertions — never use raw `Assert.*` for value comparisons
- (MUST) Mocking library: Moq — never create hand-rolled fakes for services that can be mocked
- (MUST) Test naming: `Method_Scenario_ExpectedBehavior` (e.g. `Constructor_WithNullLogger_ShouldThrowArgumentNullException`)
- (MUST) SQLite-dependent tests MUST extend `DatabaseTestBase` for consistent setup/teardown
- (MUST) CI `MIN_COVERAGE` must be set to `80` or higher in `ci.yml`
- (SHOULD) Integration tests live in `DataSpark.Tests/Integration/`; unit tests in subdirectories mirroring `DataSpark.Core`

**Evidence**: 126 test methods across 21 test files; `DatabaseTestBase` used by all DB-touching tests.

### III. Async/Await Discipline (MANDATORY)

All I/O operations MUST be async. Library code in `DataSpark.Core` MUST use `ConfigureAwait(false)`
on every `await` expression to prevent deadlocks when consumed from synchronous contexts.

- (MUST) All database, file, and network I/O must use `async`/`await` — never `.Result` or `.Wait()`
- (MUST) Every `await` in `DataSpark.Core` MUST append `.ConfigureAwait(false)`
- (MUST) All public async methods MUST accept `CancellationToken cancellationToken = default`
- (MAY) `ConfigureAwait(false)` is optional in `DataSpark.Web` and `DataSpark.Console` (ASP.NET Core and hosted services have no sync context)

**Evidence**: All 18+ Core services use `await using`; cancellation tokens present throughout.

### IV. Security — CSRF & Input Validation (MANDATORY)

The web application handles file uploads and state-changing operations that MUST be protected
against Cross-Site Request Forgery (OWASP A01).

- (MUST) Every `[HttpPost]` action MUST be decorated with `[ValidateAntiForgeryToken]`
- (MUST) Every HTML form MUST include `@Html.AntiForgeryToken()` or use tag helper `asp-antiforgery="true"`
- (MUST) File uploads MUST validate file extension AND content signature (magic bytes) — not just extension
- (MUST) All user-supplied file paths MUST be sanitized to prevent directory traversal
- (MUST) No hardcoded secrets, connection strings, or API keys in source files — use configuration/environment variables

**Evidence**: 10 `[HttpPost]` endpoints identified without CSRF protection — this principle
formalizes the fix requirement.

### V. Code Quality — Nullable & Compiler Strictness (MANDATORY)

All projects MUST enforce null-safety and treat compiler warnings as errors to prevent
null-reference exceptions and silent quality degradation.

- (MUST) `<Nullable>enable</Nullable>` in ALL `.csproj` files
- (MUST) `<TreatWarningsAsErrors>true</TreatWarningsAsErrors>` in ALL `.csproj` files
- (MUST) `<LangVersion>latest</LangVersion>` to access modern C# features
- (MUST) Use `??` null-coalescing, `ArgumentNullException.ThrowIfNull`, or `?? throw new ArgumentNullException(nameof(x))` for constructor guard clauses — never silently accept nulls
- (SHOULD) Prefer `sealed` on service implementation classes that are not designed for inheritance

**Evidence**: All 4 projects have `<Nullable>enable</Nullable>`; `TreatWarningsAsErrors` currently
missing from `DataSpark.Web` and `DataSpark.Console`.

### VI. Database Access — SQL Safety (MANDATORY)

SQLite queries MUST protect against injection at all levels. Since SQLite cannot parameterize
identifiers, a consistent escaping pattern is required.

- (MUST) All user-supplied **values** in `WHERE`, `INSERT`, `UPDATE` clauses MUST use `@parameterName` syntax
- (MUST) All **identifiers** (table names, column names) interpolated into SQL MUST use bracket-escaping: `$"SELECT * FROM [{tableName}]"` — never string concatenation (`"... FROM [" + name + "]"`)
- (MUST) Connection strings are built from `DatabaseConfiguration` objects — never constructed inline from raw user input
- (MUST) `SqliteConnection` objects MUST be disposed via `await using` or `using` blocks

**Evidence**: Parameterized queries used consistently; two concatenation instances found in
`UnifiedAnalysisService` that violate this principle.

### VII. Structured Logging (MANDATORY)

All logging MUST use structured message templates via `ILogger<T>`. The web application
MUST use Serilog as the logging provider.

- (MUST) All classes that log MUST receive `ILogger<T>` via constructor injection — never `LoggerFactory.Create` or static loggers
- (MUST) Log messages MUST use structured templates: `_logger.LogInformation("Found {Count} tables", count)` — never string interpolation in log calls
- (MUST) Log levels: `LogInformation` for normal flow, `LogWarning` for recoverable issues, `LogError` for exceptions with the exception as first argument
- (MUST) `DataSpark.Web` MUST configure Serilog via `UseSerilog()` in `Program.cs`
- (MUST NOT) `Console.Write*` or `Debug.Write*` in production code — use `ILogger` exclusively

**Evidence**: All 18+ Core services use `ILogger<T>` with structured templates; `Serilog.AspNetCore`
package present in web project.

## Approved Dependencies

The following libraries are approved for use. New dependencies require justification in a PR.

| Library | Purpose | Projects |
|---------|---------|----------|
| `Microsoft.Data.Sqlite` | SQLite database access | Core, Web |
| `CsvHelper` | CSV read/write | Core |
| `Microsoft.Extensions.*` | DI, Logging, Options, Configuration | All |
| `Serilog.AspNetCore` | Structured logging provider | Web |
| `MSTest.*` | Test framework | Tests |
| `FluentAssertions` | Test assertions | Tests |
| `Moq` | Test mocking | Tests |
| `BenchmarkDotNet` | Performance benchmarking | Benchmarks |
| `Swashbuckle.AspNetCore` | OpenAPI/Swagger UI | Web |
| `Microsoft.Data.Analysis` | DataFrame operations | Core, Web |
| `ScottPlot` | Chart rendering | Core, Web |

## Governance

- The project constitution is owned and maintained by **Mark Hazleton** (repo owner)
- All constitution amendments MUST be approved by Mark Hazleton before merging
- Proposed amendments should be submitted via `/speckit.evolve-constitution` and documented in a PR
- This constitution supersedes all other informal conventions and copilot-instructions guidance where they conflict
- Reviews for constitution compliance should occur quarterly or after significant feature additions

**Version**: 1.0.0 | **Ratified**: 2026-03-30 | **Last Amended**: 2026-03-30
